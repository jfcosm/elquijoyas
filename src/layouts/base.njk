---
---
<!DOCTYPE html>
<html lang="es" class="bg-neutral-50 text-neutral-900">
<head>
  {% set siteName = "Elqui Joyas" %}
  {% set defaultDescription = "Joyería de autor creada por Nicolás Cordero en Vicuña, Valle de Elqui, con piezas inspiradas en la naturaleza y tradiciones del río Elqui." %}
  {% set siteUrl = "https://www.elquijoyas.cl" %}
  {% set socialImage = ogImage or "https://images.unsplash.com/photo-1522312346375-d1a52e2b99b3?auto=format&fit=crop&w=1200&h=630&q=85&fm=jpg" %}
  {% set metaDescription = description or defaultDescription %}
  {% set pageTitle = siteName %}
  {% if title %}
    {% set pageTitle = title ~ " · " ~ siteName %}
  {% endif %}
  {% set canonicalUrl = siteUrl ~ page.url %}
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="{{ metaDescription }}" />
  <meta name="robots" content="index, follow" />
  <meta name="theme-color" content="#3B6B4D" />
  <title>{{ pageTitle }}</title>
  <link rel="canonical" href="{{ canonicalUrl }}" />
  <!-- Rutas de favicon corregidas (sin / inicial) -->
  <link rel="icon" type="image/svg+xml" href="favicon.svg" />
  <link rel="alternate icon" href="favicon.svg" />
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="{{ siteName }}" />
  <meta property="og:url" content="{{ canonicalUrl }}" />
  <meta property="og:title" content="{{ pageTitle }}" />
  <meta property="og:description" content="{{ metaDescription }}" />
  <meta property="og:image" content="{{ socialImage }}" />
  <meta property="og:locale" content="es_CL" />
  <meta property="og:image:type" content="image/jpeg" />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="{{ pageTitle }}" />
  <meta name="twitter:description" content="{{ metaDescription }}" />
  <meta name="twitter:image" content="{{ socialImage }}" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Work+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="preload" href="/assets/styles.css" as="style" />
  <link rel="stylesheet" href="/assets/styles.css" />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Work+Sans:wght@300;400;500;700&display=swap" rel="stylesheet">
  <!-- Rutas de CSS corregidas (sin / inicial) -->
  <link rel="preload" href="assets/styles.css" as="style" />
  <link rel="stylesheet" href="assets/styles.css" />
  <script type="application/ld+json">
    {{ {
      '@context': 'https://schema.org',
      '@type': 'JewelryStore',
      '@id': canonicalUrl,
      'name': siteName,
      'url': canonicalUrl,
      'description': metaDescription,
      'image': socialImage,
      'telephone': '+56 9 7777 1234',
      'address': {
        '@type': 'PostalAddress',
        'addressCountry': 'CL',
        'addressRegion': 'Coquimbo',
        'addressLocality': 'Vicuña'
      },
      'sameAs': [
        'https://www.instagram.com/elqui_joyas',
        'https://wa.me/56931983075'
      ]
    } | dump | safe }}
  </script>
</head>
<body class="min-h-screen bg-white text-neutral-900 antialiased">
  <a
    href="#contenido-principal"
    class="absolute left-4 top-4 -translate-y-20 rounded-full bg-primary px-5 py-2 text-sm font-semibold uppercase tracking-[0.2em] text-white focus:translate-y-0 focus:outline-none focus-visible:ring-2 focus-visible:ring-secondary"
  >Saltar al contenido principal</a>
  {% set navigationLinks = [
    {
      label: 'Inicio',
      url: '/'
    },
    {
      label: 'Taller',
      url: '/#taller'
    },
    {
      label: 'Catálogo',
      url: '/catalogo/'
    },
    {
      label: 'Contacto',
      url: '/#contacto'
    }
  ] %}
  <header class="mx-auto flex max-w-6xl items-center justify-between px-6 py-10" role="banner">
    <a href="/" class="font-display text-2xl font-semibold tracking-wide text-primary">Elqui Joyas</a>
    <nav class="flex items-center gap-6 text-sm font-medium text-neutral-700" aria-label="Navegación principal">
      <ul class="flex items-center gap-6">
        {% for item in navigationLinks %}
          <li>
            <a
              href="{{ item.url }}"
              class="hover:text-primary focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary"
              {% if page.url == item.url %}aria-current="page"{% endif %}
            >{{ item.label }}</a>
          </li>
        {% endfor %}
      </ul>
    </nav>
  </header>
  <main id="contenido-principal" class="mx-auto max-w-6xl px-6 pb-16" role="main" tabindex="-1">
    {% if intro %}
      <p class="lede">{{ intro }}</p>
    {% endif %}
    {{ content | safe }}
  </main>
  <footer class="bg-neutral-100 py-12 text-sm text-neutral-700">
    <div class="mx-auto flex max-w-6xl flex-col gap-2 px-6 sm:flex-row sm:items-center sm:justify-between">
      <p class="font-semibold text-neutral-900">© {% currentYear %} Elqui Joyas</p>
      <p class="tracking-wide">Hecho a mano en Vicuña, Valle de Elqui.</p>
    </div>
  </footer>
</body>
</html>
