---
---
<!DOCTYPE html>
<html lang="es" class="bg-neutral-50 text-neutral-900">
<head>
  {% set siteName = "El Quijoyas" %}
  {% set defaultDescription = "Joyería artesanal inspirada en La Mancha con piezas personalizables y asesoría experta." %}
  {% set siteUrl = "https://elquijoyas.com" %}
  {% set socialImage = ogImage or "https://images.unsplash.com/photo-1522312346375-d1a52e2b99b3?auto=format&fit=crop&w=1200&h=630&q=85&fm=jpg" %}
  {% set metaDescription = description or defaultDescription %}
  {% set pageTitle = siteName %}
  {% if title %}
    {% set pageTitle = title ~ " · " ~ siteName %}
  {% endif %}
  {% set canonicalUrl = siteUrl ~ page.url %}
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="{{ metaDescription }}" />
  <meta name="theme-color" content="#5B21B6" />
  <title>{{ pageTitle }}</title>
  <link rel="canonical" href="{{ canonicalUrl }}" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="alternate icon" href="/favicon.svg" />
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="{{ siteName }}" />
  <meta property="og:url" content="{{ canonicalUrl }}" />
  <meta property="og:title" content="{{ pageTitle }}" />
  <meta property="og:description" content="{{ metaDescription }}" />
  <meta property="og:image" content="{{ socialImage }}" />
  <meta property="og:image:type" content="image/jpeg" />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="{{ pageTitle }}" />
  <meta name="twitter:description" content="{{ metaDescription }}" />
  <meta name="twitter:image" content="{{ socialImage }}" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Work+Sans:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/styles.css" />
</head>
<body class="min-h-screen bg-white text-neutral-900 antialiased">
  <header class="mx-auto flex max-w-6xl items-center justify-between px-6 py-10">
    <a href="/" class="font-display text-2xl font-semibold tracking-wide text-primary">El Quijoyas</a>
    <nav class="flex items-center gap-6 text-sm font-medium text-neutral-700">
      <a href="/" class="hover:text-primary">Inicio</a>
      <a href="/catalogo/" class="hover:text-primary">Catálogo</a>
      <a href="/colecciones/" class="hover:text-primary">Colecciones</a>
      <a href="/contacto/" class="hover:text-primary">Contacto</a>
    </nav>
  </header>
  <main class="mx-auto max-w-6xl px-6 pb-16">
    {% if intro %}
      <p class="lede">{{ intro }}</p>
    {% endif %}
    {{ content | safe }}
  </main>
  <footer class="bg-neutral-100 py-12 text-sm text-neutral-700">
    <div class="mx-auto flex max-w-6xl flex-col gap-2 px-6 sm:flex-row sm:items-center sm:justify-between">
      <p class="font-semibold text-neutral-900">© {% currentYear %} El Quijoyas</p>
      <p class="tracking-wide">Hecho a mano en Castilla-La Mancha.</p>
    </div>
  </footer>
</body>
</html>
