---
layout: null
---
<!DOCTYPE html>
<html lang="es" class="bg-neutral-50 text-neutral-900">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="El Quijoyas - Joyería artesanal con carácter manchego" />
  <meta name="theme-color" content="#5B21B6" />
  <title>{% if title %}{{ title }} · {% endif %}El Quijoyas</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Work+Sans:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/styles.css" />
</head>
<body class="min-h-screen bg-white text-neutral-900 antialiased">
  <header class="mx-auto flex max-w-5xl items-center justify-between px-6 py-10">
    <a href="/" class="font-display text-2xl font-semibold tracking-wide text-primary">El Quijoyas</a>
    <nav class="flex items-center gap-6 text-sm font-medium text-neutral-700">
      <a href="/" class="hover:text-primary">Inicio</a>
      <a href="/colecciones/" class="hover:text-primary">Colecciones</a>
      <a href="/contacto/" class="hover:text-primary">Contacto</a>
    </nav>
  </header>
  <main class="mx-auto max-w-4xl px-6 pb-16">
    {% if intro %}
      <p class="lede">{{ intro }}</p>
    {% endif %}
    {{ content | safe }}
  </main>
  <footer class="bg-neutral-100 py-12 text-sm text-neutral-700">
    <div class="mx-auto flex max-w-5xl flex-col gap-2 px-6 sm:flex-row sm:items-center sm:justify-between">
      <p class="font-semibold text-neutral-900">© {{ "now" | date("yyyy") }} El Quijoyas</p>
      <p class="tracking-wide">Hecho a mano en Castilla-La Mancha.</p>
    </div>
  </footer>
</body>
</html>
